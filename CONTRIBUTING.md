# Contributing

Use Conventional Commits, keep changes focused, and run `scripts/test-all.ps1` before opening a pull request. Never commit model weights, captured media, runtime databases, or secrets. Changes to the extension/backend protocol must update both implementations and contract fixtures.
